#include <stdio.h>

/**
 * 
 * @
 * Return:
 */

void print_diagsums(int *a, int size)
{
	int num1 = 0;
	int num2 = 0;
	int recorre = 0;
	int *p1 = a;
	int *p2 = a (size - 1);

	for (; recorre < size; recorre++)
	{

	}
}
